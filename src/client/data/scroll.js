const ScrollActionTop = 'scroll-top';
const ScrollActionTo = 'scroll-to';

const ScrollEventType = 'content-scrolled';

const SCROLL_THRESHOLD = 5;

module.exports = {
  ScrollActionTop,
  ScrollActionTo,

  ScrollEventType,

  SCROLL_THRESHOLD
};
